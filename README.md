# AGV Python 项目
自动导引车（AGV）控制算法的 Python 实现。

## 环境要求
- Python 3.8+
- 虚拟环境：.venv

## 运行方法
1.  激活虚拟环境：
    ```bash
    source .venv/bin/activate  # Linux/macOS
    .venv\Scripts\activate     # Windows

2.  运行主程序
    python py.py